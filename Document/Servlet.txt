Servlet:

Servlet Content Interface

init( ServletConfig config)					
service() 									(Qly life cycle)
destroy()									Khi kết thúc chương trình
getServiceConfig() return this.config    	(Qly service Content)
getServletInfo()							(Qly service Content)

Deploy configuration in "web.xml"

<servlet> 
		<servlet-name> 	aliasname </servlet-name>
			FQN
	
 </servlet> 
 
 <servlet-mapping> 
				aliasname
				(contextRoot)/URL
		
  </servlet-mapping> 
  
  abstract                    Generic Servlet: 
  
  Servlet
  
  Tomcat(Was)   -->   Java Program
  
  
  service() : request,....
  
  HttpServlet provide support service

----------------
	Connect Servlet : 1. web.xml or 2.Context.xml
	@WebServlet("/Category/update") 			// uri
  	response.sendRedirect(path Đường dẫn) 	(dùng để quay lại trang bạn muốn)				// response phan hoi tu server đường dẫn từ thư mục góc

Cookie (be saved in client)
	?? setcookie ??? where ???
	cookie class
	response.addcookie
	request.getcookie

Session (be saved in server)
	request.getSession();
	session.setAtribute()
	session.getAtribute()
	

  
  
  